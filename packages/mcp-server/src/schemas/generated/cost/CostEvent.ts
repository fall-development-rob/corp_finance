import { z } from "zod"

export const CostEventSchema = z.object({ "cost_cents": z.number().int().describe("Dollarised cost in integer cents (no float, no Decimal)."), "event_id": z.string().uuid().describe("Stable identity for this event."), "model": z.string().describe("Model identifier (e.g. `claude-opus-4-7`)."), "surface": z.union([z.literal("cli").describe("`cfa <subcommand>` — CLI invocation, dispatched from\n`crates/corp-finance-cli/src/main.rs`."), z.literal("mcp").describe("MCP `server.tool(...)` handler — invoked by any of the\n`packages/*-mcp-server/` crates."), z.literal("skill").describe("Slash command or `.claude/skills/*` invocation — captured via\nthe MCP wrapper through which the LLM ultimately executes."), z.literal("plugin").describe("Plugin hook fire — `plugins/cfa-core/hooks/hooks.json`\n(`PreToolUse`, `PostToolUse`, `Write`, `Edit`).")]).describe("Which CFA surface produced this event."), "surface_event_id": z.string().describe("Surface-event identifier (CLI subcommand name, MCP tool name,\nplugin hook id, slash-command id)."), "tenant_id": z.union([z.string().describe("Optional tenant identifier (single-tenant default; populated by\nfederation deployments)."), z.null().describe("Optional tenant identifier (single-tenant default; populated by\nfederation deployments).")]).describe("Optional tenant identifier (single-tenant default; populated by\nfederation deployments).").optional(), "tier": z.union([z.literal("cookbook_core_only").describe("Cookbook published as `CookbookTier::CoreOnly`."), z.literal("cookbook_freemium").describe("Cookbook published as `CookbookTier::Freemium`."), z.literal("cookbook_paid_vendor").describe("Cookbook published as `CookbookTier::PaidVendor`."), z.literal("mcp_free_native").describe("MCP server classified as `McpServerTier::FreeNative`."), z.literal("mcp_free_public_with_api_key").describe("MCP server classified as `McpServerTier::FreePublicWithApiKey`."), z.literal("mcp_freemium").describe("MCP server classified as `McpServerTier::Freemium`."), z.literal("mcp_paid_vendor").describe("MCP server classified as `McpServerTier::PaidVendor`."), z.literal("unknown").describe("Surface event whose tier could not be determined at record time.\nTreated as paid-vendor for budget purposes by convention.")]).describe("Tier tag derived from CookbookTier or McpServerTier at record time."), "tokens_in": z.number().int().gte(0).describe("Input tokens consumed."), "tokens_out": z.number().int().gte(0).describe("Output tokens consumed."), "ts": z.string().datetime({ offset: true }).describe("Wall-clock timestamp.") }).describe("A single ledger row.  Emitted by the CLI tracing wrapper at subcommand\ncompletion or by the MCP server wrapper at tool-handler completion.")
