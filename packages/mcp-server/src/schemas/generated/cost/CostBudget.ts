import { z } from "zod"

export const CostBudgetSchema = z.object({ "limit_cents": z.number().int().describe("Hard cap in integer cents."), "period": z.union([z.enum(["daily","weekly","monthly"]), z.literal("total").describe("All-time cumulative.")]).describe("The period a budget covers."), "surface_filter": z.union([z.union([z.literal("cli").describe("`cfa <subcommand>` — CLI invocation, dispatched from\n`crates/corp-finance-cli/src/main.rs`."), z.literal("mcp").describe("MCP `server.tool(...)` handler — invoked by any of the\n`packages/*-mcp-server/` crates."), z.literal("skill").describe("Slash command or `.claude/skills/*` invocation — captured via\nthe MCP wrapper through which the LLM ultimately executes."), z.literal("plugin").describe("Plugin hook fire — `plugins/cfa-core/hooks/hooks.json`\n(`PreToolUse`, `PostToolUse`, `Write`, `Edit`).")]).describe("One of the four CFA execution surfaces.\n\nEvery CFA runtime event lands on exactly one of these four boundaries.\nThe wire form is `snake_case` — for the current variant set this is\nbyte-identical to lowercase, but `snake_case` is the canonical choice\nto match the rest of the Phase-26 enum vocabulary (see `cost::TierTag`,\n`cost::BudgetPeriod`)."), z.null()]).describe("Optional surface filter; `None` covers all surfaces.").optional(), "threshold_pct": z.number().int().gte(0).lte(255).describe("Warning threshold as a percentage (0..=100).  Crossing this fires the\n`budget_threshold_crossed` integration event but does not block."), "tier_filter": z.union([z.union([z.literal("cookbook_core_only").describe("Cookbook published as `CookbookTier::CoreOnly`."), z.literal("cookbook_freemium").describe("Cookbook published as `CookbookTier::Freemium`."), z.literal("cookbook_paid_vendor").describe("Cookbook published as `CookbookTier::PaidVendor`."), z.literal("mcp_free_native").describe("MCP server classified as `McpServerTier::FreeNative`."), z.literal("mcp_free_public_with_api_key").describe("MCP server classified as `McpServerTier::FreePublicWithApiKey`."), z.literal("mcp_freemium").describe("MCP server classified as `McpServerTier::Freemium`."), z.literal("mcp_paid_vendor").describe("MCP server classified as `McpServerTier::PaidVendor`."), z.literal("unknown").describe("Surface event whose tier could not be determined at record time.\nTreated as paid-vendor for budget purposes by convention.")]).describe("Tier classification persisted on a `CostEvent`.  Combines `CookbookTier`\n(deploy-time) and `McpServerTier` (runtime) into a single tag space so the\nledger can be aggregated by tier without joining across two enums.\n\nPer RUF-COST-002 / RUF-COST-INV-002, this enum is **derived** from those\ntwo source-of-truth tier enums; no parallel tiering logic lives here."), z.null()]).describe("Optional tier filter; `None` covers all tiers.").optional() }).describe("Budget definition.  Keys on `(surface_filter, tier_filter, period)`\ninside the ledger.")
