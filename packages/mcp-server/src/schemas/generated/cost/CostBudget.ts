import { z } from "zod"

export const CostBudgetSchema = z.object({ "limit_cents": z.number().int().describe("Hard cap in integer cents."), "period": z.union([z.enum(["daily","weekly","monthly"]), z.literal("total").describe("All-time cumulative.")]).describe("The period a budget covers."), "surface_filter": z.union([z.enum(["cli", "mcp", "skill", "plugin"]).describe("One of the four CFA execution surfaces.\n\nEvery CFA runtime event lands on exactly one of these four boundaries.\nThe wire form is `snake_case` — for the current variant set this is\nbyte-identical to lowercase, but `snake_case` is the canonical choice\nto match the rest of the Phase-26 enum vocabulary (see `cost::TierTag`,\n`cost::BudgetPeriod`)."), z.null()]).describe("Optional surface filter; `None` covers all surfaces.").optional(), "threshold_pct": z.number().int().gte(0).lte(255).describe("Warning threshold as a percentage (0..=100).  Crossing this fires the\n`budget_threshold_crossed` integration event but does not block."), "tier_filter": z.union([z.enum(["cookbook_core_only", "cookbook_freemium", "cookbook_paid_vendor", "mcp_free_native", "mcp_free_public_with_api_key", "mcp_freemium", "mcp_paid_vendor", "unknown"]).describe("Tier classification persisted on a `CostEvent`.  Combines `CookbookTier`\n(deploy-time) and `McpServerTier` (runtime) into a single tag space so the\nledger can be aggregated by tier without joining across two enums.\n\nPer RUF-COST-002 / RUF-COST-INV-002, this enum is **derived** from those\ntwo source-of-truth tier enums; no parallel tiering logic lives here."), z.null()]).describe("Optional tier filter; `None` covers all tiers.").optional() }).describe("Budget definition.  Keys on `(surface_filter, tier_filter, period)`\ninside the ledger.")
