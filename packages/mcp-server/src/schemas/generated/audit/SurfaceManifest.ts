import { z } from "zod"

export const SurfaceManifestSchema = z.object({ "command_args": z.any().describe("Canonical JSON of the command arguments / tool input. The hasher\ncanonicalises this further (sorts keys, strips whitespace) before\nfolding into the hash."), "output_paths": z.array(z.string()).describe("Output file paths produced by the event. Sorted lexicographically\ninside the hasher so insertion order does not affect the hash."), "surface": z.enum(["cli", "mcp", "skill", "plugin"]).describe("Which of the four CFA surfaces produced this event."), "surface_event_id": z.string().describe("Surface-specific event identifier — CLI subcommand name (e.g.\n`workflow.audit`), MCP tool name (e.g. `dcf_model`), slash-command id,\nor plugin hook id (e.g. `pre_tool_use`).") }).describe("Canonical manifest of a single surface event, hashed to produce\n`surface_audit_hash`.\n\nPer ADR-017 §1, the hash domain is the surface event's static manifest:\nthe `surface` enum value, an opaque identifier for the event, the command\narguments / tool input value, and the set of output paths the event\nproduced. The `serde_json::Value` for `command_args` is canonicalised\n(keys sorted, no insignificant whitespace) before hashing — see\n[`compute_surface_audit_hash`] for the canonicalisation step.")
