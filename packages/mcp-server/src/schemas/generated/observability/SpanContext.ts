import { z } from "zod"

export const SpanContextSchema = z.object({ "run_id": z.union([z.string().uuid().describe("Run id (uuid-v7) generated at the root of a CFA surface event. The\naudit manifest, run summary, cost ledger row, and span tree all share\nthis id for cross-surface correlation."), z.null().describe("Run id (uuid-v7) generated at the root of a CFA surface event. The\naudit manifest, run summary, cost ledger row, and span tree all share\nthis id for cross-surface correlation.")]).describe("Run id (uuid-v7) generated at the root of a CFA surface event. The\naudit manifest, run summary, cost ledger row, and span tree all share\nthis id for cross-surface correlation.").optional(), "surface": z.enum(["cli", "mcp", "skill", "plugin"]).describe("Originating surface of the event."), "surface_event_id": z.string().describe("Per-event identifier — CLI subcommand name, MCP tool name, hook id,\nor slash-command id. NOT a uuid; this is the *kind* of event."), "tenant_id": z.union([z.string().describe("Optional tenant/customer id; redacted to \"j.smith\"-style on the wire\nwhen populated from the OS user identity (ADR-017 §4 / RUF-OBS-003)."), z.null().describe("Optional tenant/customer id; redacted to \"j.smith\"-style on the wire\nwhen populated from the OS user identity (ADR-017 §4 / RUF-OBS-003).")]).describe("Optional tenant/customer id; redacted to \"j.smith\"-style on the wire\nwhen populated from the OS user identity (ADR-017 §4 / RUF-OBS-003).").optional() }).describe("Lightweight context propagated alongside a span. Spans themselves are\nowned by the `tracing` runtime; `SpanContext` is the value-type that\nobservability callers and downstream surfaces (cost ledger, audit\nmanifest, memory) can read without taking a tracing dependency.")
