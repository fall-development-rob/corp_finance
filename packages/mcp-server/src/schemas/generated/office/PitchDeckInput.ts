import { z } from "zod"

export const PitchDeckInputSchema = z.object({ "agenda": z.array(z.string()).describe("Bullet labels for the Agenda slide."), "business_overview": z.array(z.string()).describe("3-6 business overview bullets."), "conclusion": z.array(z.string()).describe("3-6 closing / conclusion bullets."), "date": z.string().describe("Deck date, e.g. \"May 2026\"."), "deal_name": z.string().describe("Company or deal name — rendered as the title slide heading."), "executive_summary": z.array(z.string()).describe("3-6 executive-summary bullets."), "financial_highlights": z.array(z.array(z.string())).describe("Financial highlights table; first row is the header row."), "market_overview": z.array(z.string()).describe("3-6 market overview bullets."), "process_timeline": z.array(z.array(z.any()).min(2).max(2)).describe("Process timeline rows as (milestone, date) pairs."), "returns_summary": z.array(z.array(z.any()).min(2).max(2)).describe("Returns summary rows as (metric, value) pairs."), "subtitle": z.string().describe("Secondary title line, e.g. \"Project Atlas — Confidential\".") }).describe("All fields required to build a canonical sell-side IB pitch deck.")
