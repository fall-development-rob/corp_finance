import { z } from "zod"

export const IcPresentationInputSchema = z.object({ "date": z.string(), "deal_name": z.string(), "investment_thesis": z.array(z.string()).describe("3-6 investment thesis bullets."), "key_metrics": z.array(z.array(z.any()).min(2).max(2)).describe("(metric label, value) pairs for the key-metrics table."), "mitigants": z.array(z.string()).describe("3-6 mitigant bullets."), "presenter": z.string(), "recommendation": z.string().describe("e.g. \"APPROVE — $250M EQUITY CHECK\""), "returns_table": z.array(z.array(z.string())).describe("Returns table; first row is treated as headers."), "risks": z.array(z.string()).describe("3-6 risk bullets."), "timeline": z.array(z.array(z.any()).min(2).max(2)).describe("(milestone, date) pairs for the process timeline.") }).describe("Structured input for an Investment Committee presentation.")
