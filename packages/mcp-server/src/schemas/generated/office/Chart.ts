import { z } from "zod"

export const ChartSchema = z.object({ "anchor_col": z.number().int().gte(0).describe("0-indexed column of the chart's top-left anchor cell."), "anchor_row": z.number().int().gte(0).describe("0-indexed row of the chart's top-left anchor cell."), "kind": z.enum(["line","bar","column","pie"]).describe("Chart variety supported by the xlsx writer."), "series": z.array(z.object({ "categories_range": z.string().describe("A1 absolute range for the category (X-axis) data."), "name": z.string().describe("Series display name."), "values_range": z.string().describe("A1 absolute range for the values (Y-axis) data.") }).describe("One data series within a chart, specified via A1-style absolute ranges.\nExample: `categories_range = \"Sheet1!$A$2:$A$10\"`.")).describe("At least one series is required for a valid chart."), "title": z.union([z.string(), z.null()]).default(null) }).describe("A chart object embedded in a worksheet at `(anchor_row, anchor_col)`.")
