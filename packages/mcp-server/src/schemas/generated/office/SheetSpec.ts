import { z } from "zod"

export const SheetSpecSchema = z.object({ "cell_formats": z.array(z.any()).describe("Cell-level format overlays applied after all data/formula writes.").default([]), "charts": z.array(z.any()).describe("Charts to embed in this worksheet.").default([]), "column_widths": z.array(z.number()).describe("Per-column widths in Excel units; empty = auto.").default([]), "formulas": z.array(z.any()).describe("Ad-hoc formula cells (can reference any row/col on the sheet).").default([]), "frozen_panes": z.union([z.any(), z.null()]).describe("Freeze rows above + cols left of (row, col).").default(null), "headers": z.array(z.string()).describe("First row, rendered bold.").default([]), "name": z.string().describe("Worksheet tab name (max 31 chars per Excel)."), "rows": z.array(z.array(z.any())).describe("Data rows written beneath the headers row.").default([]) }).describe("Description of one worksheet tab.")
