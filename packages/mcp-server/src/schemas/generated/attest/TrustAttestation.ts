import { z } from "zod"

export const TrustAttestationSchema = z.object({ "attestation_id": z.string().uuid(), "capabilities": z.array(z.string()), "expires_at": z.string().datetime({ offset: true }), "issued_at": z.string().datetime({ offset: true }), "issuer_tenant": z.string(), "public_key": z.string().describe("hex-encoded ed25519 verifying key (64 hex chars / 32 bytes raw)"), "signature": z.string().describe("hex-encoded ed25519 signature (128 hex chars / 64 bytes raw)"), "subject_tenant": z.string() }).describe("A signed claim that the issuer tenant grants the subject tenant a set of\ncapabilities for a fixed time window. Verifiable offline by anyone with\nthe issuer's ed25519 verifying key.\n\nCanonical signing payload (must match exactly across issue/verify):\n\n```text\nv1|<issuer_tenant>|<subject_tenant>|<capabilities_csv_sorted>|<issued_at_rfc3339>|<expires_at_rfc3339>\n```\n\nThe `capabilities` vector is sorted lexicographically before being CSV-joined\nfor the canonical payload, but the field on the wire keeps the caller's order.")
