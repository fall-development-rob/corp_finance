{
  "name": "@robotixai/corp-finance-mcp",
  "version": "1.0.0",
  "description": "Institutional-grade corporate finance calculations as MCP tools",
  "repository": {
    "type": "git",
    "url": "https://github.com/fall-development-rob/corp_finance.git",
    "directory": "packages/mcp-server"
  },
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "corp-finance-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "schemas:export:office": "cd ${COREREPO:-../../../corp-finance-core} && cargo test -p corp-finance-core --features schema_gen,office --test export_office_schemas",
    "schemas:export:attest": "cd ${COREREPO:-../../../corp-finance-core} && cargo test -p corp-finance-core --features schema_gen,federation --test export_attest_schemas",
    "schemas:export:memory": "cd ${COREREPO:-../../../corp-finance-core} && cargo test -p corp-finance-core --features schema_gen,memory --test export_memory_schemas",
    "schemas:export:audit": "cd ${COREREPO:-../../../corp-finance-core} && cargo test -p corp-finance-core --features schema_gen,audit --test export_audit_schemas",
    "schemas:export:cost": "cd ${COREREPO:-../../../corp-finance-core} && cargo test -p corp-finance-core --features schema_gen,cost --test export_cost_schemas",
    "schemas:export:observability": "cd ${COREREPO:-../../../corp-finance-core} && cargo test -p corp-finance-core --features schema_gen,observability --test export_observability_schemas",
    "schemas:gen:office": "npm run schemas:export:office && tsx src/tools/agent_infrastructure/zod_transform.ts office",
    "schemas:gen:attest": "npm run schemas:export:attest && tsx src/tools/agent_infrastructure/zod_transform.ts attest",
    "schemas:gen:memory": "npm run schemas:export:memory && tsx src/tools/agent_infrastructure/zod_transform.ts memory",
    "schemas:gen:audit": "npm run schemas:export:audit && tsx src/tools/agent_infrastructure/zod_transform.ts audit",
    "schemas:gen:cost": "npm run schemas:export:cost && tsx src/tools/agent_infrastructure/zod_transform.ts cost",
    "schemas:gen:observability": "npm run schemas:export:observability && tsx src/tools/agent_infrastructure/zod_transform.ts observability",
    "schemas:gen:all": "npm run schemas:gen:office && npm run schemas:gen:attest && npm run schemas:gen:memory && npm run schemas:gen:audit && npm run schemas:gen:cost && npm run schemas:gen:observability",
    "check:surface-parity": "tsx src/tools/agent_infrastructure/surface_parity.ts",
    "cookbooks:validate": "tsx src/tools/agent_infrastructure/cookbook.ts",
    "wasm:build": "tsx src/tools/agent_infrastructure/wasm_build.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@robotixai/corp-finance-bindings": "file:../bindings",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "json-schema-to-zod": "^2.8.1",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
